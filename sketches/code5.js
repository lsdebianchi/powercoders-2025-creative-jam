// Variables globales ici.

function setup_5() {
  // Ceci s’exécute une fois.
}

function draw_5() {
  // Ceci s’exécute à chaque frame.
  background5(0, 255, 0);
  textSize(30);
  text("B2 For the Emperor", width5 / 2, height5 / 2);
}

// N’oubliez pas d’utiliser la version numérotée des fonctions communes :
// width5 height5 mouseX5 mouseY5 background5()
